hydra:
  job:
    config:
      # configuration for the ${hydra.job.override_dirname} runtime variable
      override_dirname:
        kv_sep: '='
        item_sep: ','
        exclude_keys: []
dataset:
  image_root_folder: ./datasets/fashion_mnist/images
  groundtruth_label_file: ./datasets/fashion_mnist/cls_labels.npy
  label_file: ./datasets/fashion_mnist/cls_labels.npy
  sample_size: 128
  name: fashion_mnist
model:
  name: MobileNet
  initialize: True
training:
  log_file: logs/losses.log
  checkpoint: checkpoints/model.pth
  batch_size: 128
  num_epochs: 100
  n_clusters: 10
  img_size: 32
  reassign: 4
  use_original_labels: False
  optimizer:
    name: sgd
    lr: 0.05
    momentum: 0.9
    wd: -5
  pca:
    component_size: None
debug_root: debug/
